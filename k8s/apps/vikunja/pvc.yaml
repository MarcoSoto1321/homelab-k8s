apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vikunja-pvc
  namespace: apps
  labels:
    app: vikunja
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
