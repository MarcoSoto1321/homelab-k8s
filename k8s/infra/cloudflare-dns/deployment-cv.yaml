apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflare-ddns-cv
  namespace: infra
  labels:
    app: cloudflare-ddns
    instance: cv
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflare-ddns
      instance: cv
  template:
    metadata:
      labels:
        app: cloudflare-ddns
        instance: cv
    spec:
      containers:
        - name: cloudflare-ddns
          image: favonia/cloudflare-ddns:latest
          imagePullPolicy: Always
          env:
            - name: CLOUDFLARE_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflare-ddns-token
                  key: api-token
            - name: DOMAINS
              value: "cv.marcosoto.dev"
            - name: PROXIED
              value: "true"
            - name: RECORD_COMMENT
              value: "managed-by-ddns-cv"
            - name: MANAGED_RECORDS_COMMENT_REGEX
              value: "^managed-by-ddns-cv$"
            - name: UPDATE_CRON
              value: "*/5 * * * *"
            - name: IP6_PROVIDER
              value: "none"
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 32Mi
          securityContext:
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      restartPolicy: Always
      tolerations:
        - key: "node-role.kubernetes.io/control-plane"
          operator: "Exists"
          effect: "NoSchedule"
